import { EnumBaseColors } from '@/theme/entities/enum-base-colors';
import styled from 'styled-components';

export const StyledPasswordlessLogin = styled.div`
  width: 100%;
  margin: 20px 0;
  border-bottom: 1px solid ${EnumBaseColors.LightGray};
`;
